service: ci-cd

provider:
  name: aws
  runtime: nodejs14.x
  deploymentBucket:
  name: my.lambda.deployment.bucket
  stage: dev
  region: us-east-1

functions:
  hello:
    handler: src/handler.generateRandomNumber
